part of '../screens/home_screen.dart';

class _TextWidget extends StatelessWidget {
  const _TextWidget();

  @override
  Widget build(BuildContext context) {
    return const Text('Hello, GetX!', style: TextStyle(fontSize: 24));
  }
}
